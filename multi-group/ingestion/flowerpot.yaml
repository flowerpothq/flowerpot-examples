pipelines:
  extract:
    run: "python3 scripts/extract_api.py"
    env:
      OUTPUT_DIR: "../data/raw"

  normalize:
    run: "python3 scripts/normalize.py"
    after: [extract]
    python:
      deps: ["orjson"]
    env:
      INPUT_DIR: "../data/raw"
      OUTPUT_DIR: "../data/normalized"
